@page "{id:int}"
@model Frontend.Pages.Resource.DeleteModel
@{
    ViewData["Title"] = "Удаление";
}

<h1>@ViewData["Title"]</h1>

<h3>Вы уверены, что хотите удалить этот ресурс?</h3>
<div>
    <h4>Ресурс</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Наименование</dt>
        <dd class="col-sm-10">@Model.Resource.Name</dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Resource.Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-page="./Index" class="btn btn-secondary">Отмена</a>
    </form>
</div>