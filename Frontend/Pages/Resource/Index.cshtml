@page
@model Frontend.Pages.Resource.IndexModel
@{
    ViewData["Title"] = "Ресурсы";
}

<h1>@ViewData["Title"]</h1>

<div class="mb-3">
    <a class="btn btn-success" asp-page="./Create">Добавить</a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Наименование</th>
            <th>Статус</th>
            <th>Действия</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var resource in Model.Resources)
        {
            <tr>
                <td>@resource.Name</td>
                <td>@resource.Status</td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@resource.Id">Редактировать</a> |
                    <a asp-page="./Delete" asp-route-id="@resource.Id" class="text-danger">Удалить</a>
                </td>
            </tr>
        }
    </tbody>
</table>